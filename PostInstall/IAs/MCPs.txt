

# Instalar Node.js y npm si no los tienes
sudo pacman -S nodejs npm

# Verificar la instalación
mcp --version


# Crear directorio de configuración
mkdir -p ~/.config/mcp

# Crear archivo de configuración básico
cat > ~/.config/mcp/config.json << EOF
{
  "servers": {},
  "tools": {}
}
EOF


ejemplo:
# Instalar MCP localmente
npm install @modelcontextprotocol/server-filesystem


GitHub: Para gestionar issues, pull requests y otras interacciones con repositorios en GitHub.
Filesystem: Permite operaciones seguras con archivos en tu sistema local, con control de acceso configurable. Ideal para leer, escribir o buscar archivos.
Grep MCP: Permite hacer búsquedas con expresiones regulares (regex) y semánticas a través de grandes repositorios de código públicos.




He instalado, Ver como instalar más abajo:

npm install @playwright/mcp@latest
npm install @upstash/context7-mcp



Se puede instalar global:
npm install -g @modelcontextprotocol/server-filesystem
Desventajas: Requiere permisos de admin, puede tener conflictos de versiones


npm list -g
npm list


Configuramos:
nano ~/.config/mcp/config.json

{
  "servers": {
    "filesystem": {
      "command": "node",
      "args": ["/home/c2mismo/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js", "/home/c2mismo/PROYECTOS/"]
    }
  },
  "tools": {}
}

nano ~/.config/Claude/claude_desktop_config.json

{
  "mcpServers": {
    "filesystem": {
      "command": "node",
      "args": ["/home/c2mismo/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js", "/home/c2mismo/PROYECTOS/"]
    }
  }
}

NOTA:
# Archivo genérico MCP (para MCP Inspector, otras herramientas)
~/.config/mcp/config.json → "servers"

# Archivo específico Claude Desktop
~/.config/Claude/claude_desktop_config.json → "mcpServers"




# Probar que el servidor arranca (saldrás con Ctrl+C)
node ~/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js /home/c2mismo

node ~/node_modules/@playwright/mcp/index.js /home/c2mismo

@playwright/mcp@latest

# Abrir MCP Inspector conectado a tu directorio
npx @modelcontextprotocol/inspector node ~/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js /home/c2mismo




# Ver si hay errores en los logs de Claude Desktop
journalctl --user -f | grep -i claude

# O revisar si el servidor MCP arranca manualmente
node ~/node_modules/@modelcontextprotocol/server-filesystem/dist/index.js /home/c2mismo



**********************************************************************
Instalaciones:

@playwright/mcp@latest
necesitamos google-chrome-stable

abrimos google-chrome-stable
descargamos:
https://github.com/microsoft/playwright-mcp/releases
playwright-mcp-extension-n.n.nn.zip

descomprimimos en:
/home/c2mismo/Documents/extensionChromeMCPplaywright/

chrome://extensions/
Habilite el 'Modo de desarrollador' (alternar en la esquina superior derecha)
Haga clic en 'Cargar desempaquetado' y seleccione el directorio de la extensión


npm install @playwright/mcp@latest

    "playwright": {
      "command": "npx",
      "args": [
        "@playwright/mcp@latest",
        "--extension",
        "--executable-path=/usr/bin/google-chrome-stable"
      ],
      "disabled": false,
      "alwaysAllow": []
    }

El paso de chrome://extensions/, hay que hacerlo cada vez que se ejecuta el chrome,
en cuanto se le da acceso el MCP el chrome mueve le extension para el MPC a /tmp, creo que por seguradad

Realiza una prueba integral del funcionamiento del MCP de Playwright navegando a la página https://wiki.archlinux.org/title/Backlight, localiza específicamente la sección que contiene ejemplos de uso del comando setpci, identifica el ejemplo concreto que se proporciona, extrae el comando completo con todos sus parámetros y opciones, analiza detalladamente cada componente del comando explicando la función de cada parámetro, el significado de los valores hexadecimales utilizados, el propósito del registro PCI que se está modificando, y proporciona el contexto completo de por qué este comando específico es relevante para el control del brillo de pantalla en sistemas Linux, incluyendo cualquier advertencia o precaución mencionada en la documentación.




Realiza una prueba integral del funcionamiento del MCP de Playwright busca informcación del paquete fx_cast en la página https://aur.archlinux.org/






























Opción 1: Usar el paquete oficial (recomendado)

# Desinstalar la versión del AUR
paru -R claude-desktop-bin

# Descargar el AppImage oficial desde Claude.ai
mkdir -p ~/Applications
cd ~/Applications
wget https://claude.ai/download/desktop/linux -O Claude-Desktop.AppImage

# Hacer ejecutable
chmod +x Claude-Desktop.AppImage

# Ejecutar
./Claude-Desktop.AppImage




Opción 2: Instalar desde npm (alternativa)

# Instalar Claude Desktop desde npm
npm install -g @anthropic-ai/claude-desktop

# Ejecutar
claude-desktop


+++++++++++++++++++++++++
Descarga directa oficial:

Ir a https://claude.ai/download
Descargar el .deb o .AppImage oficial
Usar debtap para convertir .deb a package de Arch






Otros para probar:

playwright

Context7

GitHub

ClickUp

















